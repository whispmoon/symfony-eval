{% extends 'base.html.twig' %}

{% block content %}
    <h1>Sciences list</h1>
    <hr>
    <div class="table-responsive">
        <table class="table table-stripped table-condensed">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for science in sciences %}
                <tr>
                    <td>
                        <a href="{{ path('science_show', { 'id': science.id }) }}">
                            {{ science.id }}
                        </a>
                    </td>
                    <td>{{ science.title }}</td>
                    <td>{{ science.description }}</td>
                    <td>
                        <ul class="list-inline">
                            <li>
                                <a href="{{ path('science_show', { 'id': science.id }) }}"
                                    class="btn btn-xs btn-primary">
                                    Show
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('science_edit', { 'id': science.id }) }}"
                                    class="btn btn-xs btn-warning">
                                    Edit
                                </a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <hr>
    <ul class="list-inline">
        <li>
            <a href="{{ path('science_new') }}" class="btn btn-sm btn-success">
                Create a new science
            </a>
        </li>
    </ul>
{% endblock %}
